header.header
  .header-top
    .container
      .header-left
        b BOOKSTORE
      // End .header-left
      .header-right
        ul.top-menu
          li
            a(href='#') Links
            ul
              li
                a(href='/api/v1/users')
                  i.icon
                  - if(user&& user.iam_role==0)
                    | Bạn đang Đăng nhập dưới tư cách Admin. Bạn có thể chuyển đến trang quản lí tại đây
                  - if(user&& user.iam_role==1)
                    a
                      | Bạn đang Đăng nhập dưới tư cách Moderator.
                  - if(user&& user.iam_role==2)
                    a
                      | Bạn đang Đăng nhập dưới tư cách member.

              li
              - if (!user)
                li
                  a.nav-link(href='/api/v1/auth') Login
              - else
                li
                  a(href="###").userInfor
                    - if(user.avatarUrl)
                        img(style='width: 40px; height: 40px; border-radius: 50%' src=`${user.avatarUrl}`)
                        span= user.name
                    - else
                        img(style='width: 40px; height: 40px; border-radius: 50%' src="https://scontent.fhan14-5.fna.fbcdn.net/v/t1.30497-1/453178253_471506465671661_2781666950760530985_n.png?stp=dst-png_s200x200&_nc_cat=1&ccb=1-7&_nc_sid=136b72&_nc_ohc=ANaUE6Y_shwQ7kNvgEDF7Bf&_nc_ht=scontent.fhan14-5.fna&oh=00_AYBuYkGVVF3ZQEo0td2RNTKwS_m0COnLcvKg1AZlZm6Q-A&oe=66F245FA")
                        span= user.name
        // End .top-menu
      // End .header-right